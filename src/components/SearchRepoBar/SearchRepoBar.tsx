import React from 'react';
import './SearchRepoBar.css';

const SearchRepoBar: React.FC = () => {
  return <input type="search" placeholder="Search for User`s Repositories"/>;
}

export default SearchRepoBar;